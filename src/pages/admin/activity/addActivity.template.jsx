import React from 'react';
import AutoForm from '../../../components/forms/autoform/autoform';

const render=function () {
    return <AutoForm formOption={this.state.formOption} />;
}

export default render;
