import React from 'react';
import BodeTreeGrid from '../../../components/uis/tables/bode-treegrid/bode-treegrid';

const render=function () {
    return <BodeTreeGrid treeGridOptions={this.state.treeGridOptions} />;
}

export default render;
